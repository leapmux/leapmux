/** A data visualization the model described as a chart specification. */
export interface ChartRequest { spec: string, title?: string, description?: string }
export type { ChartResult } from '../chartResult'
