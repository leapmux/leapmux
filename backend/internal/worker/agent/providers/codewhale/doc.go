// Package codewhale implements the Codewhale provider. It runs
// `codewhale app-server --http` for each agent and drives the runtime API over
// REST calls and one server-sent event stream for the agent's thread.
package codewhale
