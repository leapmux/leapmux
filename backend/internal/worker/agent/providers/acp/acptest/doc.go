// Package acptest holds the test support that the tests of package acp and of
// each ACP provider share. It does not import package acp, so the in-package
// tests of acp can import it.
package acptest
