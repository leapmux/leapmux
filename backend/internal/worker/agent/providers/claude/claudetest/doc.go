// Package claudetest holds start functions that run a Claude agent over a mock
// process, for a test that needs a running agent and no model. StartEcho runs a
// process that echoes each input back, and StartSilent runs one that reads its
// input and answers nothing.
package claudetest
